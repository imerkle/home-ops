---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./hr.yaml
  - ./oci.yaml
  - ./backend.yaml
  - ./aigatewayroute.yaml    # flux-mcp.domain.com (hostname-based)
  - ./aiservicebackend.yaml  # Required by AIGatewayRoute
  # - ./mcproute.yaml          # mcp.domain.com/mcp (path-based aggregation)