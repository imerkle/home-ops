# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: pg-vector-rw
#   namespace: pg
# spec:
#   ingressClassName: ${INGRESS1}
#   rules:
#   - host: &host "pg-vector-rw.${DOMAIN1}"
#     http:
#       paths:
#       - pathType: Prefix
#         path: "/"
#         backend:
#           service:
#             name: pg-vector-rw
#             port:
#               number: 5432
#   tls:
#     - hosts:
#         - *host

---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pg-vector-rw
  # namespace: pg
spec:
  parentRefs:
    - name: internal
      namespace: kube-system
  hostnames:
    - "pg-vector-rw.${DOMAIN1}"
  rules:
    - backendRefs:
        - name: pg-vector-rw
          port: 5432
