id: whatsapp
url: http://mautrix-whatsapp:8000
as_token: $AS_TOKEN
hs_token: $HS_TOKEN
sender_localpart: whatsappbot
rate_limited: false
namespaces:
  users:
    - regex: ^@whatsappbot:${DOMAIN1_ESCAPED}$
      exclusive: true
    - regex: ^@whatsapp_.*:${DOMAIN1_ESCAPED}$
      exclusive: true
de.sorunome.msc2409.push_ephemeral: true
receive_ephemeral: true
