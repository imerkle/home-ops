---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app game-fetcher-mtui
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  maxHistory: 2
  install:
    disableWait: true
    remediation:
      retries: 3
  upgrade:
    disableWait: true
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      game-fetcher-mtui:
        annotations:
          reloader.stakater.com/auto: "true"
        pod:
          annotations:
            vault.security.banzaicloud.io/vault-env-daemon: "true"
        containers:
          app:
            image:
              repository: zot.${DOMAIN1}/game-fetcher
              tag: d21dd757f9e637d11ebaad85d4c265b46f210020
            env:
              PORT: &port "3000"
              RAWG: "vault:secret/data/ext_services#RAWG"
              OIDC_CLIENT_ID: "vault:secret/data/ext_services#OIDC_CLIENT_ID"
              OIDC_CLIENT_SECRET: "vault:secret/data/ext_services#OIDC_CLIENT_SECRET"
              BASE_URL: "https://mtui-game-fetcher.${DOMAIN1}"
              SOURCES: '[{"name":"FitGirl Local","url":"file://sources/fitgirl.json"}, {"name":"Dodi Local","url":"file://sources/dodi.json"}, {"name":"Kazumi Local","url":"file://sources/kazumi.json"}, {"name":"Xatab Local","url":"file://sources/xatab.json"}]'
              INDEX_INTERVAL: "1h"
              OIDC_ISSUER: "https://zitadel.${DOMAIN1}"
              OIDC_REDIRECT_URL: "https://mtui-game-fetcher.${DOMAIN1}/auth/callback"
              OIDC_SESSION_KEY: "super-secret-session-key-change-me"
              DATABASE_URL: "postgres://$${vault:database/creds/game-fetcher-role#username}:$${vault:database/creds/game-fetcher-role#password}@pg-default-rw.db.svc.cluster.local:5432/gamefetcher?sslmode=disable"
              TEMPORAL_HOST: "temporal.dev-system.svc.cluster.local:7233"
              TEMPORAL_NAMESPACE: "default"
              TEMPORAL_TASK_QUEUE: "game-fetcher"
              QBITTORRENT_URL: "http://qbittorrent.home-system.svc.cluster.local:8080"
              QBITTORRENT_USERNAME: "admin"
              QBITTORRENT_PASSWORD: "adminadmin"
              JDOWNLOADER_URL: "http://jdownloader.home-system.svc.cluster.local:3128"
              MEILISEARCH_URL: "http://meilisearch.db.svc.cluster.local:7700"
              MEILISEARCH_API_KEY: "super-secret-master-key"
              AUTH_SERVER_ADDR: auth-server.prod-system:50051
              SETTINGS_SERVER_ADDR: settings-server.prod-system:50051
              THUMBNAIL_SERVER_ADDR: thumbnail-service.prod-system:50051
              PROWLARR_URL: http://prowlarr.home-system.svc.cluster.local:9696
              PROWLARR_API_KEY: "vault:secret/data/prowlarr#API_KEY"
              DOWNLOAD_GAME_FOLDER: games
              DOWNLOAD_MOVIE_FOLDER: movies
              SCAN_GAME_PATH: /games
              SCAN_MOVIE_PATH: /movies
              KETO_READ_URL: http://keto-read.keto.svc.cluster.local:80
              KETO_WRITE_URL: http://keto-write.keto.svc.cluster.local:80
              PYLOAD_API_KEY: pl_11QjyHY-c2WpHMI7KAiKPmiI3cagS0m-VzOe6vU7Bw0lg
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /readyz
                    port: 3000
                  initialDelaySeconds: 15
                  periodSeconds: 10
                  timeoutSeconds: 3
                  failureThreshold: 3
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /readyz
                    port: 3000
                  initialDelaySeconds: 5
                  periodSeconds: 10
                  timeoutSeconds: 3
                  failureThreshold: 3
              startup:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz
                    port: 3000
                  initialDelaySeconds: 2
                  periodSeconds: 5
                  timeoutSeconds: 3
                  failureThreshold: 30
            resources:
              requests:
                cpu: 10m
                memory: 16Mi
              limits:
                memory: 300Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      tolerations:
        - key: dedicated
          operator: Equal
          value: tekton-runner
          effect: NoSchedule
    service:
      app:
        controller: game-fetcher-mtui
        ports:
          http:
            port: *port
            appProtocol: kubernetes.io/ws
    serviceMonitor:
      app:
        enabled: true
        endpoints:
          - port: http
            scheme: http
            path: /metrics
            interval: 1m
            scrapeTimeout: 10s
        serviceName: game-fetcher-mtui
    route:
      app:
        hostnames:
          - "mtui-game-fetcher.${DOMAIN1}"
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /v1/traces
            backendRefs:
              - name: otel-collector
                namespace: observability
                port: 4318
          - matches:
              - path:
                  type: PathPrefix
                  value: /auth
              - path:
                  type: PathPrefix
                  value: /api/auth
            backendRefs:
              - name: auth-server
                namespace: prod-system
                port: 3000
          - backendRefs:
              - port: *port
                name: game-fetcher-mtui
    persistence:
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
      media:
        existingClaim: copyparty-hdd
        globalMounts:
          - path: /games
            subPath: downloads/games
          - path: /movies
            subPath: downloads/movies
