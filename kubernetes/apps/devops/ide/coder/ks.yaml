---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app coder
spec:
  components:
    - ../../../../components/volsync
  targetNamespace: dev-system
  path: ./kubernetes/apps/devops/ide/coder
  dependsOn:
    - name: zitadel-app
      namespace: zitadel
    - name: vault
      namespace: vault
    - name: pg-clusters-default
      namespace: db
  postBuild:
    substitute:
      APP: *app
      # GATUS_PATH: /web/index.html
      VOLSYNC_CAPACITY: 5Gi
      # REDIRECT_URI: https://coder.${DNS_MAIN}/api/v2/users/oidc/callback
      # LOGOUT_URI: https://coder.${DNS_MAIN}/api/v2/users/logout