---
apiVersion: infra.contrib.fluxcd.io/v1alpha2
kind: Terraform
metadata:
  name: forgejo-zitadel-oidc
spec:
  interval: 1h
  retryInterval: 1h
  retryStrategy: StaticInterval
  approvePlan: auto
  path: ./terraform/zitadel-oidc
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  vars:
    - name: app_name
      value: "forgejo-oidc"
    - name: redirect_uris
      value:
        - "https://forgejo.${DOMAIN1}/user/oauth2/Ok8Sh/callback"
  writeOutputsToSecret:
    name: forgejo-oidc-creds
    outputs:
      - client_id
      - client_secret
