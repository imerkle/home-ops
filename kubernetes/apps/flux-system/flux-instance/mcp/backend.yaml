---
# Backend resource required by AIServiceBackend
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: flux-mcp-backend
spec:
  endpoints:
    - fqdn:
        hostname: flux-operator-mcp.flux-system.svc.cluster.local
        port: 9090
