---
# yaml-language-server: $schema=https://kubernetes-schemas.ok8.sh/helm.toolkit.fluxcd.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app coder
spec:
  chart:
    spec:
      chart: coder
      version: 2.29.1
      sourceRef:
        kind: HelmRepository
        name: coder-v2
        namespace: dev-system
  values:
    coder:
      resources:
        requests:
         cpu: 175m
#         memory: 600Mi
        limits:
         memory: 600Mi
      podAnnotations:
        reloader.stakater.com/auto: "true"
        vault.security.banzaicloud.io/vault-role: "default"
        vault.security.banzaicloud.io/vault-env-daemon: "true"
      env:
        - name: CODER_ACCESS_URL
          value: https://coder.${DOMAIN1}
        - name: CODER_WILDCARD_ACCESS_URL
          value: "*.coder.${DOMAIN1}"
        - name: CODER_OIDC_SIGN_IN_TEXT
          value: "Sign In with Ok8Sh"
        - name: CODER_PROMETHEUS_ENABLE
          value: "true"
        # - name: CODER_DISABLE_PASSWORD_AUTH
          # value: "true"
        - name: CODER_PG_CONNECTION_URL
          value: "postgres://$${vault:database/creds/coder-role#username}:$${vault:database/creds/coder-role#password}@pg-default-rw.db.svc.cluster.local:5432/coder?sslmode=disable"
        - name: CODER_OIDC_ALLOW_SIGNUPS
          value: "true"
        - name: CODER_OIDC_ISSUER_URL
          value: ${OAUTH2_ISSUER}
        - name: CODER_OIDC_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: coder-oidc-creds
              key: client_id
        - name: CODER_OIDC_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: coder-oidc-creds
              key: client_secret
        - name: CODER_POSTGRES_HOST
          value: "pg-default-rw.db.svc.cluster.local"
        - name: CODER_POSTGRES_PORT
          value: "5432"
        - name: CODER_POSTGRES_USER
          value: "$${vault:database/creds/coder-role#username}"
        - name: CODER_POSTGRES_PASSWORD
          value: "$${vault:database/creds/coder-role#password}"
        - name: CODER_POSTGRES_MAIN_DB
          value: "coder"
      volumes:
        - name: syncthing-data
          persistentVolumeClaim:
            claimName: syncthing-data-shared
      volumeMounts:
        - name: syncthing-data
          mountPath: /home/coder/shared
          subPath: dotfiles
      # extraContainers:
      #         - name: docker-sidecar
      #           image: docker:24-dind
      #           securityContext:
      #             privileged: true  # Required for Docker to run inside a container
      #           env:
      #             - name: DOCKER_TLS_CERTDIR
      #               value: "" # Disables TLS for easier communication on localhost
      #           resources:
      #             requests:
      #               cpu: 100m
      #               memory: 512Mi
      #             limits:
      #               cpu: 1000m
      #               memory: 2048Mi
      #           volumeMounts:
      #             - name: docker-storage
      #               mountPath: /var/lib/docker
      # volumes:
      #   - name: docker-storage
      #     emptyDir: {}
      # ingress:
      #   enable: true
      #   className: internal
      #   host: "coder.${DOMAIN1}"
      #   wildcardHost: "*.coder.${DOMAIN1}"
