---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: network

components:
  - ../../components/alerts

resources:
  - ./namespace.yaml
  # gateway
  - ./gateway/envoy-gateway/ks.yaml
  # dns
  - ./dns/cloudflare-dns/ks.yaml
  # - ./dns/unifi-dns/ks.yaml
  # certificates
  - ./certs/certificates/ks.yaml
  # tunnel
  # - ./tunnel/cloudflare-tunnel/ks.yaml
  # - ./tunnel/echo/ks.yaml
