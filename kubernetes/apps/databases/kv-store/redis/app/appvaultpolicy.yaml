apiVersion: home.arpa/v1alpha1
kind: AppVaultPolicy
metadata:
  name: redis-vault
  namespace: db
spec:
  serviceAccount: redis
  policy: |
    path "secret/data/redis" {
      capabilities = ["read"]
    }
