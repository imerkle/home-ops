id: telegram
url: http://mautrix-telegram:29317
as_token: $${vault:secret/data/mautrix#as_token3}
hs_token: $$${vault:secret/data/mautrix#as_token3}
sender_localpart: telegrambot
rate_limited: false
namespaces:
  users:
    - regex: ^@telegrambot:matrix\.${DOMAIN1_ESCAPED}$
      exclusive: true
    - regex: ^@telegram_.*:matrix\.${DOMAIN1_ESCAPED}$
      exclusive: true
de.sorunome.msc2409.push_ephemeral: true
receive_ephemeral: true
