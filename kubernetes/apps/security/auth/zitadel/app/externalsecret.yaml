apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: zitadel-db-creds
  namespace: security
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: zitadel-db-creds
    creationPolicy: Owner
    template:
      type: Opaque
  data:
    - secretKey: username
      remoteRef:
        key: database/static-creds/zitadel-static
        property: username
    - secretKey: password
      remoteRef:
        key: database/static-creds/zitadel-static
        property: password
