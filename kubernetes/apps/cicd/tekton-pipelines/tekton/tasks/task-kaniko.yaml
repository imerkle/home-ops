apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: kaniko-build
  labels:
    app.kubernetes.io/version: "0.7"
  annotations:
    tekton.dev/pipelines.minVersion: "0.43.0"
    tekton.dev/categories: Image Build
    tekton.dev/tags: image-build
    tekton.dev/displayName: "Build and upload container image using Kaniko"
    tekton.dev/platforms: "linux/amd64,linux/arm64,linux/ppc64le"
    tekton.dev/deprecated: "true"
spec:
  description: >-
    This Task builds a simple Dockerfile with kaniko and pushes to a registry.
    This Task stores the image name and digest as results, allowing Tekton Chains to pick up
    that an image was built & sign it.
  params:
    - name: IMAGE
      description: Name (reference) of the image to build.
    - name: DOCKERFILE
      description: Path to the Dockerfile to build.
      default: ./Dockerfile
    - name: CONTEXT
      description: The build context used by Kaniko.
      default: ./
    - name: EXTRA_ARGS
      type: array
      default: []
    - name: PROTOCOL
      description: Protocol to use for the image URL (http or https).
      default: https
    - name: BUILDER_IMAGE
      description: The image on which builds will run (default is v1.5.1)
      default: gcr.io/kaniko-project/executor:v1.23.2
    - name: WRITER_IMAGE
      description: The image on which the write-url step will run (default is docker.io/library/bash:5.1.4@sha256:c523c636b722339f41b6a431b44588ab2f762c5de5ec3bd7964420ff982fb1d9)
      default: docker.io/library/bash:5.1.4@sha256:c523c636b722339f41b6a431b44588ab2f762c5de5ec3bd7964420ff982fb1d9
  workspaces:
    - name: source
      description: Holds the context and Dockerfile
    - name: dockerconfig
      description: Includes a docker `config.json`
      optional: true
      mountPath: /kaniko/.docker
  results:
    - name: IMAGE_DIGEST
      description: Digest of the image just built.
    - name: IMAGE_URL
      description: URL of the image just built.
  steps:
    # Step 1: If the repo contains a .build-args file at the context root,
    # write each KEY=VAL line into /tekton/env/ so the next step inherits them
    # as environment variables. Kaniko then satisfies Dockerfile ARG declarations
    # that have no default value from the environment (same as --build-arg KEY).
    # This keeps the pipeline fully generic — any repo just drops a .build-args file.
    - name: prepare-build-args
      image: docker.io/library/alpine:3.18
      script: |
        #!/bin/sh
        set -e
        BUILD_ARGS_FILE="$(workspaces.source.path)/$(params.CONTEXT)/.build-args"
        if [ ! -f "$BUILD_ARGS_FILE" ]; then
          echo "[prepare] No .build-args file found, skipping."
          exit 0
        fi
        echo "[prepare] Injecting build args from .build-args into /tekton/env:"
        while IFS= read -r line || [ -n "$line" ]; do
          # Strip carriage returns (CRLF support)
          line=$(echo "$line" | tr -d '\r')
          # Skip empty/comment lines
          case "$line" in ''|'#'*) continue ;; esac
          # Ensure line contains '='
          case "$line" in *=*) ;; *) continue ;; esac
          
          KEY="${line%%=*}"
          VAL="${line#*=}"
          # Strip leading/trailing whitespaces from KEY
          KEY=$(echo "$KEY" | tr -d '[:space:]')
          if [ -n "$KEY" ]; then
            echo "  $KEY=***"
            printf '%s' "$VAL" > "/tekton/env/$KEY"
          fi
        done < "$BUILD_ARGS_FILE"
        # Always inject a unique CACHEBUST to force fresh npm install of git deps
        mkdir -p /tekton/env
        CACHEBUST=$(date +%s)
        echo "  CACHEBUST=$CACHEBUST"
        printf '%s' "$CACHEBUST" > "/tekton/env/CACHEBUST"
    # Step 2: Run Kaniko. Uses args (not script) so Tekton can expand EXTRA_ARGS[*].
    # Env vars from /tekton/env are automatically available here, so Dockerfile
    # ARG declarations without a default pick them up from the environment.
    - name: build-and-push
      workingDir: $(workspaces.source.path)
      image: $(params.BUILDER_IMAGE)
      args:
        - $(params.EXTRA_ARGS[*])
        - --dockerfile=$(params.DOCKERFILE)
        - --context=$(workspaces.source.path)/$(params.CONTEXT)
        - --destination=$(params.IMAGE)
        - --digest-file=$(results.IMAGE_DIGEST.path)
      securityContext:
        runAsUser: 0
    - name: write-url
      image: $(params.WRITER_IMAGE)
      script: |
        set -e
        image="$(params.IMAGE)"
        protocol="$(params.PROTOCOL)"
        printf "%s://%s" "$protocol" "$image" | tee "$(results.IMAGE_URL.path)"
