---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: debian-install-media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 8Gi
