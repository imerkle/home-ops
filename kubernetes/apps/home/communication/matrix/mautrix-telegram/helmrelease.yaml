apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mautrix-telegram
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    controllers:
      mautrix-telegram:
        labels:
          postgres: "true"
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          copy-config:
            image:
              repository: public.ecr.aws/docker/library/python
              tag: 3.11-alpine
            command: ["python3", "-c"]
            args:
              - |
                import os, shutil
                for f in os.listdir('/tmp/config'):
                    if not f.startswith('.'):
                        shutil.copy(os.path.join('/tmp/config', f), '/data')
                with open('/data/config.yaml', 'r') as f:
                    content = f.read()
                content = content.replace(f"{chr(36)}{chr(123)}DB_USER{chr(125)}", os.environ['DB_USER'])
                content = content.replace(f"{chr(36)}{chr(123)}DB_PASS{chr(125)}", os.environ['DB_PASS'])
                with open('/data/config.yaml', 'w') as f:
                    f.write(content)
            env:
              DB_USER: "$${vault:database/creds/mautrix-telegram-role#username}"
              DB_PASS: "$${vault:database/creds/mautrix-telegram-role#password}"
        containers:
          app:
            image:
              repository: dock.mau.dev/mautrix/telegram
              tag: v0.15.3
            command: ["python3", "-m", "mautrix_telegram"]
            args: [ "-c", "/data/config.yaml", "--no-update" ]
            workingDir: /data
            env:
              TZ: America/New_York
              DB_USER: "$${vault:database/creds/mautrix-telegram-role#username}"
              DB_PASS: "$${vault:database/creds/mautrix-telegram-role#password}"
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
            resources:
              requests:
                cpu: 10m
                memory: 128Mi
              limits:
                memory: 512Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          http:
            port: 29317
    route:
      app:
        annotations:
          gatus.home-operations.com/endpoint: |-
            conditions: ["[STATUS] == 404"]
        hostnames:
          - "{{ .Release.Name }}.x3y.space"
        parentRefs:
          - name: envoy-internal
            namespace: networking
    persistence:
      config:
        type: configMap
        name: mautrix-telegram-config
        advancedMounts:
          mautrix-telegram:
            copy-config:
              - path: /tmp/config
      data:
        existingClaim: mautrix-telegram
        globalMounts:
          - path: /data
      tmpfs:
        type: emptyDir
        advancedMounts:
          mautrix-telegram:
            app:
              - path: /tmp
                subPath: tmp
