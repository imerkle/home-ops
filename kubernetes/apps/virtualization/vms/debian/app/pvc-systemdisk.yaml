---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: debian-systemdisk
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORE1}
  resources:
    requests:
      storage: 30Gi
