apiVersion: home.arpa/v1alpha1
kind: AppVaultPolicy
metadata:
  name: auth-server-vault
  namespace: prod-system
spec:
  serviceAccount: auth-server
  policy: |
    path "secret/data/ext_services" {
      capabilities = ["read"]
    }
