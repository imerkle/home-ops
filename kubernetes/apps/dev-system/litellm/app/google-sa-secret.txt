# User needs to create this secret manually or via another ExternalSecret if the JSON is in Vault
# kubectl create secret generic google-sa-secret --from-file=credentials.json=/path/to/key.json -n dev-system
